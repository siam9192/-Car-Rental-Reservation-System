import React from 'react';

type TSectionContainerProps = {
  children: React.ReactNode;
};

const SectionContainer = ({}: TSectionContainerProps) => {
  return <div></div>;
};

export default SectionContainer;
